name: Build

# Compiles the Lazarus/FreePascal parts of Cheat Engine on a Windows runner and
# uploads the portable distribution as a workflow artifact.
#
# This workflow is also called by release.yml, so the artifact attached to a release
# is produced by exactly the same steps as an ordinary CI build.

on:
  push:
    branches: [master]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches: [master]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:
    inputs:
      build_all:
        description: 'Force a full recompile of every unit (--build-all)'
        type: boolean
        default: false
  workflow_call:
    inputs:
      version:
        description: 'Version string used in the archive name'
        type: string
        required: true
      build_all:
        type: boolean
        default: false

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

env:
  LAZARUS_VERSION: '2.2.2'
  FPC_VERSION: '3.2.2'
  LAZARUS_DIR: 'C:\lazarus'
  FPC_DIR: 'C:\FPC'
  TOOLCHAIN_CACHE: 'C:\toolchain-cache'

jobs:
  build:
    name: Windows / Lazarus + FPC
    runs-on: windows-latest
    timeout-minutes: 150

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Work out the version string
        id: version
        shell: pwsh
        run: |
          $version = '${{ inputs.version }}'
          if (-not $version) {
            # Ordinary CI build: short SHA is enough to tell two builds apart.
            $version = "$(git rev-parse --short HEAD)"
          }
          "value=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          Write-Host "version = $version"

      # ~180 MB of installers and source tarball. Caching them keeps every build off
      # the critical path of an upstream host having a bad day.
      - name: Restore cached toolchain downloads
        uses: actions/cache@v4
        with:
          path: ${{ env.TOOLCHAIN_CACHE }}
          key: toolchain-fpc-${{ env.FPC_VERSION }}-lazarus-${{ env.LAZARUS_VERSION }}

      - name: Install FPC and build lazbuild
        shell: pwsh
        run: |
          .github\build\install-toolchain.ps1 `
            -FpcVersion '${{ env.FPC_VERSION }}' `
            -LazarusVersion '${{ env.LAZARUS_VERSION }}' `
            -FpcDir '${{ env.FPC_DIR }}' `
            -LazarusDir '${{ env.LAZARUS_DIR }}' `
            -CacheDir '${{ env.TOOLCHAIN_CACHE }}'

      - name: Build
        shell: pwsh
        run: |
          $buildAll = '${{ inputs.build_all }}' -eq 'true'
          if ($buildAll) {
            .github\build\build.ps1 -LazarusDir '${{ env.LAZARUS_DIR }}' -BuildAll
          } else {
            .github\build\build.ps1 -LazarusDir '${{ env.LAZARUS_DIR }}'
          }

      - name: Package the portable distribution
        id: package
        shell: pwsh
        run: .github\build\package.ps1 -Version '${{ steps.version.outputs.value }}'

      - name: Upload the portable build
        uses: actions/upload-artifact@v4
        with:
          name: cheat-engine-portable-${{ steps.version.outputs.value }}
          path: |
            dist/*.zip
            dist/SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 30

      - name: Collect the toolchain log
        if: always()
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build-logs | Out-Null
          $log = Join-Path '${{ env.TOOLCHAIN_CACHE }}' 'make-lazbuild.log'
          if (Test-Path -LiteralPath $log) { Copy-Item -LiteralPath $log -Destination build-logs\ }

      # Compiler transcripts are the only way to diagnose a failed target after the
      # runner is gone, so keep them whether or not the build succeeded.
      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ steps.version.outputs.value }}
          path: build-logs/
          if-no-files-found: warn
          retention-days: 14
